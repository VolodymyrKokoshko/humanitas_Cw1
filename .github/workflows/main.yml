on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: ${{ secrets.DIRECTORY }}
      with:
        server: ${{ secrets.HOST }}
        username: ${{ secrets.LOGIN }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
